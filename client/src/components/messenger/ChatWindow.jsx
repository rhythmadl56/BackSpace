const ChatWindow = ({ title = 'Chat' }) => {
  return (
    <section className="chat-window">
      <h3>{title}</h3>
      <p>Conversation will appear here.</p>
    </section>
  )
}

export default ChatWindow
